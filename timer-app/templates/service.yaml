apiVersion: v1
kind: Service
metadata:
  name: {{ include "timer.fullname" . }}
  labels:
    {{- include "timer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "timer.selectorLabels" . | nindent 4 }}
